# Lambda package dependencies (install into build dir for zip)
# Keep lean for Academy Lab cold starts
boto3>=1.35.0
httpx>=0.27.0
pydantic-settings>=2.6.0
pydantic>=2.0
feedparser>=6.0.11
# Jobs import app.core.deps.build_job_context, which currently imports FastAPI.
fastapi>=0.115.0
