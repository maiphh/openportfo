# Lambda package dependencies (install into build dir for zip)
# Keep lean for Academy Lab cold starts
boto3>=1.35.0
httpx>=0.27.0
pydantic-settings>=2.6.0
pydantic>=2.0
feedparser>=6.0.11
# fastapi/uvicorn not required for jobs-only Lambda
