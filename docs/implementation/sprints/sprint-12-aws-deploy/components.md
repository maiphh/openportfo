# Sprint 12 — Components

1. DynamoDB tables (or single-table) for all entities
2. S3 data + (later) frontend buckets
3. Cognito user pool + app client (+ Google optional)
4. Wire prod adapters in deps (AUTH_MODE=cognito)
5. Beanstalk deploy FastAPI + instance profile
6. Lambda package jobs + EventBridge cron (no FX in jobs)
7. Athena: one documented query on snapshots
8. .env.example without secrets
