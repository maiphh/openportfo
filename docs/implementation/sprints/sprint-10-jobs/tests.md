# Sprint 10 — Tests

1. news job writes matched items
2. disabled news flag → skip + JobRun reason
3. snapshot writes storage + repo
4. price job updates cache
5. no ExchangeRateClient usage (mock assert not called)
