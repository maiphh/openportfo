# Legacy workflow prompts

These singular `.workflow` agent and command prompts predate the executable project cycle and are not discovered by the installed OpenCode configuration. The authoritative cross-agent process is now `AGENTS.md` plus `.workflows/feature-cycle.md`; verification is `.workflows/verify.ps1`.

Keep these files only as historical role detail. New SDLC behavior, gates, and skills belong in `.workflows/` and `.agents/skills/` so Codex and other repository-aware agents share the same contract.
