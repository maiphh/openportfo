# Workflow stress-test baseline — 2026-08-28

Baseline taken before the autonomous cycle was added.

| Lane | Result | Evidence |
| --- | --- | --- |
| Backend | Pass | 533 passed, 5 skipped; four warnings |
| Frontend lint | Pass | ESLint exited 0 |
| Frontend unit | Pass | 59 files, 399 tests |
| Frontend production build | Fail | TypeScript unreachable comparison in `frontend/lib/portfolio-charts.ts:125` |
| Playwright | Missing | `@playwright/test` and browser binaries were not installed |
| LocalStack runtime | Pass | DynamoDB and S3 healthy; 11 expected tables and bucket present |
| LocalStack application tests | Missing | Existing `RUN_AWS_TESTS` smoke test targets real AWS STS, which this Compose service does not enable |
| Ports command | Fail | Documented `Select-String -Recurse` is not valid PowerShell syntax |
| Artifact resolution | Fail | Commands assumed `BL-027-design.md`, while design/research artifacts include a slug |
| OpenCode agent discovery | Fail | `.workflow/agents` was not discovered; configured default `orchestrator` did not exist |
| Nested frontend guidance | Fail | `frontend/AGENTS.md` routed ordinary UI work through an unavailable Python/Vnstock skill loader |
| Test side effect | Fail | Importing `vnstock` generated `backend/AGENTS.md` in a background thread during pytest |

The new cycle addresses these failures with a tool-neutral root contract, repository-scoped skills, executable gates, wildcard-safe artifact conventions, Playwright coverage, and a read-only LocalStack contract probe.
